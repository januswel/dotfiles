printf('*%s*',expand('%:t'))	Plugin to show example
                                                Last change: 2014 Sep 27.

SYNOPSIS						printf('*%s-synopsis*',expand('%:t:r'))
>
	:Example "It's an example"

DESCRIPTION						printf('*%s-description*',expand('%:t:r'))

Write simple descriptions here.

DEPENDENCIES						printf('*%s-dependencies*',expand('%:t:r'))

{only available when your VIM has the |+example| feature}

This plugin require following autoload file[s].

https://github.com/januswel/jwlib.vim/blob/master/autoload/jwlib/buf.vim

See https://github.com/januswel/jwlib.vim .

The external command "example" is required.
	http://example.com/

PROVIDES						printf('*%s-provides*',expand('%:t:r'))

Mappings:
<Leader>example or <Plug>(example)
	Descriptions for mappings.

Commands:
:Example {word}
	Descriptions for commands.

Augroups:
'%:t:r'
	All of the autocmds provided by this plugin is located in printf('"%s".',expand('%:t:r'))
	To disable in a certain instance of VIM, hit the following command: >

		:autocmd! '%:t:r'

Functions:
jwlib#Example()
	Descriptions for functions.

SETTINGS						printf('*%s-settings*',expand('%:t:r'))

This plugin doesn't have any settings.

DISABLES						printf('*%s-disables*',expand('%:t:r'))

Variables:
printf('g:loaded_%s',expand('%:t:r'))
	When exists, does nothing.  Use to disable this plugin.

printf('g:no_%s_maps',expand('%:t:r'))
	When non-zero, doesn't register any effective mappings.

g:no_plugin_maps
	When non-zero, doesn't register any effective mappings.  This variable
	may have an affect on mappings of other plugins.  Use
	printf('"g:no_%s_maps"',expand('%:t:r')) to disable only mappings of
	this plugin.

KOWN ISSUES						printf('*%s-knownissues*',expand('%:t:r'))

There are no issues.

ACKNOWLEDGMENT 						printf('*%s-acknowledgment*',expand('%:t:r'))


==============================================================================

Author
	janus_wel <janus.wel.3@gmail.com>
Copyright
	(c) 2014 by janus_wel
License
	This material may be distributed only subject to the terms and
	conditions set forth in the Open Publication License, v1.0 or later
	(the latest version is presently available at
	http://www.opencontent.org/openpub/).

vim:tw=78:ts=8:noet:ft=help:norl:
