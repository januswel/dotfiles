---
default:
    # bash
    - .bash_aliases
    - .bash_profile
    - .bashrc

    # zsh
    - .zprofile
    - .zshrc

    # ls
    - .dir_colors

    # git
    - .config/git

    # tmux
    - .tmux.conf

    # vim
    - .vim
    - .vimrc
    - .gvimrc
    - .config/nvim

    # npm
    - .npmrc

    # EditorConfig
    - .editorconfig

# for windows settings
windows:
    - source: .vim
      target: vimfiles
